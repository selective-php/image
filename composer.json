{
  "name": "odan/image",
  "type": "library",
  "description": "Image library",
  "keywords": [
    "library"
  ],
  "homepage": "https://github.com/odan/image",
  "license": "MIT",
  "require": {
    "php": "^7.0",
    "ext-gd": "*",
    "ext-exif": "*"
  },
  "require-dev": {
    "php": "^7.0",
    "phpunit/phpunit": "^6"
  },
  "require-dev": {
    "phpunit/phpunit": "^6.0"
  },
  "scripts": {
    "test": "phpunit",
    "test-coverage": "phpunit --coverage-clover build/logs/clover.xml --coverage-html build/coverage",
    "check-style": "phpcs -p --standard=PSR2 --runtime-set ignore_errors_on_exit 1 --runtime-set ignore_warnings_on_exit 1 src tests",
    "fix-style": "phpcbf -p --standard=PSR2 --runtime-set ignore_errors_on_exit 1 --runtime-set ignore_warnings_on_exit -n --no-patch 1 src tests"
  },
  "autoload": {
    "psr-4": {
      "Odan\\Image\\": "src/Image",
      "Odan\\Image\\Test\\": "tests"
    }
  },
  "autoload-dev": {
    "psr-4": {
      "App\\Test\\": "tests"
    }
  },
  "config": {
    "sort-packages": true
  }
}
